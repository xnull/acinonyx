package acinonyx.security

import org.scalatest.FunSuite

class HomeScenarioTest extends FunSuite {

  /**
    * нужна потсоянная связь с клиентом, если он пропал больше чем на ${timeout} (30) секунд то начинать истерить, нужен heartbeat
    *
    */
  test("alarm") {

  }
}
